import React from 'react';
import PropTypes from 'prop-types';
import styles from './ImageDropZone.css';

const ImageDropZone = ({

}) => {
  return (
    <div className={styles.wrapper}>
      <div className={styles['image-preview']}>
        <img src="" />
      </div>
    </div>
  );
};

export default ImageDropZone;
